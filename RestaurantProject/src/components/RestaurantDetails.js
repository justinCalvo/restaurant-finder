import React from 'react';
import { View, Text } from 'react-native';

const RestaurantDetails = ({ route }) => {
  // console.log('this is where I want to be: ', route.params.restaurants);
  return (
    <View>
      <Text>oops</Text>
    </View>
  );
};

export default RestaurantDetails;
